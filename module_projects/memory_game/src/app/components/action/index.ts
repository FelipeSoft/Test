import { ActionButton } from "./ActionButton";
import { ActionMoves } from "./ActionMoves";
import { ActionRoot } from "./ActionRoot";
import { ActionTimer } from "./ActionTimer";

export const Action = {
    Root: ActionRoot,
    Timer: ActionTimer,
    Moves: ActionMoves,
    Button: ActionButton
}